# Local Development

Use the repository's pinned package manager and toolchain. Run workspace install checks before starting Next.js, backend, Anchor, Sui, or SVM builds. Never deploy with placeholder keys or program IDs.
