# Powerchain contracts

This directory contains deployment policy and release manifests for the Anchor program in `programs/powerchain`.
Production program IDs and authorities must be supplied through reviewed deployment manifests; placeholder IDs must never be treated as deployed addresses.
