{
  "name": "@powerchain/solana-contracts",
  "version": "1.0.0-beta.1",
  "private": true,
  "description": "Canonical PowerChain Solana workspace for Anchor programs, SPL assets and deployment resources.",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "test:unit": "pnpm exec vitest run",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "deploy:mainnet-beta": "anchor deploy --provider.cluster mainnet",
    "idl:build": "anchor idl build",
    "lint": "cargo fmt --all -- --check"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@solana/spl-token": "^0.4.14",
    "@solana/web3.js": "^1.98.4",
    "@metaplex-foundation/mpl-token-metadata": "^3.4.0",
    "@metaplex-foundation/umi": "^1.4.1",
    "zod": "^4.0.0"
  }
}
