---
name: powerchain-solana
version: 1.0.0-beta.1
---

# PowerChain Solana skill

Use the canonical `programs/powerchain` Anchor workspace. Verify cluster, program ID, wallet, and transaction simulation before writes. Never treat the placeholder system program ID as deployable. Keep IDL and Cargo versions synchronized through `env!("CARGO_PKG_VERSION")`.
