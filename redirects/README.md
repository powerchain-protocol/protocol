
# Redirect rules

The canonical redirect list is stored in `rules.json` and mirrored in the public web application's `next.config.ts`.

Production deployments should replace localhost destinations with the configured dashboard hostname.
