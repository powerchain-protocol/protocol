
# Deployment configuration

Application-specific runtime configuration lives under each app's `config/` directory.

- `apps/web/config`: site metadata, routes, feature flags
- `apps/backend/src/config`: environment, server, and security configuration
- `vercel.json`: web deployment defaults
- `redirects/`: portable redirect definitions
