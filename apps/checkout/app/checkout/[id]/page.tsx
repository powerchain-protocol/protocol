
import {checkoutFixture} from "@powerchain/checkout";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const s={...checkoutFixture,id};const total=s.lineItems.reduce((a,b)=>a+b.quantity*b.unitAmount,0);return <main className="grid min-h-screen place-items-center p-5"><section className="w-full max-w-xl rounded-[28px] border bg-white p-8 shadow-xl"><div className="flex justify-between"><b>POWERCHAIN</b><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs text-emerald-700">Secure checkout</span></div><h1 className="mt-8 text-3xl font-semibold">Complete your payment</h1><div className="mt-6">{s.lineItems.map(i=><div className="flex justify-between rounded-xl bg-slate-50 p-4" key={i.id}><span>{i.name}</span><b>{(i.quantity*i.unitAmount).toFixed(2)} {i.currency}</b></div>)}</div><div className="mt-5 flex justify-between border-t pt-5 text-xl"><b>Total</b><strong>{total.toFixed(2)} USD</strong></div><select className="mt-6 h-12 w-full rounded-xl border px-3">{s.settlementAssets.map(a=><option key={a}>{a}</option>)}</select><button className="mt-4 w-full rounded-xl bg-emerald-800 py-3 text-white">Pay securely</button></section></main>}
