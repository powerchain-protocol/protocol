"use client";import{createContext,useContext,useMemo,useState}from"react";type K="marketplace"|"renewables"|"crowdfunding"|"payments"|"solana"|"wallet"|"ai"|"iot"|"depin"|"hardware";const seed:Record<K,boolean>={marketplace:true,renewables:true,crowdfunding:true,payments:true,solana:true,wallet:true,ai:true,iot:true,depin:true,hardware:true};const C=createContext<{services:Record<K,boolean>;setService:(k:K,v:boolean)=>void}|null>(null);export function PlatformServicesProvider({children}:{children:React.ReactNode}){const[services,setServices]=useState(seed);const value=useMemo(()=>({services,setService:(k:K,v:boolean)=>setServices(s=>({...s,[k]:v}))}),[services]);return<C.Provider value={value}>{children}</C.Provider>}export function usePlatformServices(){const v=useContext(C);if(!v)throw new Error("PlatformServicesProvider missing.");return v}