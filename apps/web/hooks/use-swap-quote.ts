
"use client";import {useEffect,useState} from "react";import {fetchSwapQuote,type SwapQuote} from "@/services/swap";
export function useSwapQuote(input:{enabled:boolean;environment:"mock"|"devnet"|"mainnet";inputMint:string;outputMint:string;amount:string;slippageBps:number}){const [quote,setQuote]=useState<SwapQuote>();const [loading,setLoading]=useState(false);const [error,setError]=useState<string>();useEffect(()=>{if(!input.enabled)return;const timer=setTimeout(async()=>{setLoading(true);try{setQuote(await fetchSwapQuote(input));setError(undefined)}catch(e){setError(e instanceof Error?e.message:"Quote unavailable")}finally{setLoading(false)}},350);return()=>clearTimeout(timer)},[input.enabled,input.environment,input.inputMint,input.outputMint,input.amount,input.slippageBps]);return {quote,loading,error}}
