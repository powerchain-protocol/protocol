
import {Header} from "@/components/layout/header";import {Footer} from "@/components/layout/footer";import {renewableTokenUseCases} from "@powerchain/renewables";
export default function Page(){return <><Header/><main className="mx-auto max-w-[1300px] px-5 py-20"><p className="text-xs font-black uppercase tracking-[.16em] text-emerald-700">PWRC token</p><h1 className="mt-3 text-6xl font-semibold">Utility for renewable infrastructure.</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-500">The initial PWRC reference price is $0.000002. Mainnet pricing remains unavailable until a live mint and market source are configured.</p><div className="mt-10 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{renewableTokenUseCases.map(item=><article className="rounded-[28px] border bg-white p-6 shadow-[0_20px_60px_rgba(15,23,42,.065)]" key={item.id}><h2 className="text-xl font-bold">{item.title}</h2><p className="mt-3 text-sm leading-6 text-slate-500">{item.description}</p><small className="mt-5 block font-bold text-emerald-700">{item.requiresMainnet?"Mainnet operation":"Available for testing"}</small></article>)}</div></main><Footer/></>}
