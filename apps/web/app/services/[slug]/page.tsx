import {notFound} from "next/navigation"; import {ArrowRight,Check} from "lucide-react"; import {services} from "@/data/services"; import {Header} from "@/components/layout/header"; import {Footer} from "@/components/layout/footer"; import {Badge} from "@/components/ui/badge"; import {Button} from "@/components/ui/button"; import {Card} from "@/components/ui/card";
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();const Icon=s.icon;return <><Header/><main className="container py-20"><Badge>Powerchain services</Badge><div className="mt-6 grid gap-10 lg:grid-cols-2"><div><Icon className="size-14 text-primary"/><h1 className="mt-6 text-6xl font-semibold tracking-tight">{s.name}</h1><p className="mt-6 max-w-xl text-lg leading-8 text-muted-foreground">{s.description}</p><Button className="mt-8">Speak with an expert <ArrowRight className="size-4"/></Button></div><Card className="p-8"><h2 className="text-2xl font-semibold">What is included</h2><div className="mt-6 grid gap-4">{["Discovery and architecture","Implementation roadmap","Technical integration","Security review","Operational enablement"].map(x=><div className="flex gap-3" key={x}><Check className="text-primary"/><span>{x}</span></div>)}</div></Card></div></main><Footer/></>}