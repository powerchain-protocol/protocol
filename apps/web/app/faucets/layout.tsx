import type { Metadata } from "next";
export const metadata: Metadata = { title: "Developer Faucet", description: "Claim PWRC test tokens on Solana devnet." };
export default function FaucetLayout({ children }: { children: React.ReactNode }) { return children; }
