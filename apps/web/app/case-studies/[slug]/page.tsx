
import {notFound} from "next/navigation";import {Header} from "@/components/layout/header";import {Footer} from "@/components/layout/footer";import {CASE_STUDIES} from "@/data/home";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=CASE_STUDIES.find(x=>x.slug===slug);if(!c)notFound();return <><Header/><main className="mx-auto max-w-4xl px-5 py-20"><small className="font-bold text-emerald-700">{c.company}</small><h1 className="mt-4 text-6xl font-semibold">{c.title}</h1><p className="mt-8 text-xl leading-9 text-slate-600">{c.summary}</p><div className="mt-10 rounded-3xl bg-emerald-950 p-8 text-white"><small>Measured result</small><strong className="mt-3 block text-4xl text-lime-300">{c.result}</strong></div></main><Footer/></>}
