
import {Header} from "@/components/layout/header";import {Footer} from "@/components/layout/footer";import {RENEWABLE_ASSETS} from "@/data/renewable-assets";import {summarizeRenewables} from "@powerchain/renewables";
export default function Page(){const summary=summarizeRenewables(RENEWABLE_ASSETS);return <><Header/><main className="pc-shell min-h-[820px] px-5 py-14"><div className="mx-auto max-w-[1500px]"><p className="text-xs font-black uppercase tracking-[.16em] text-emerald-700">Renewable infrastructure</p><h1 className="mt-3 text-5xl font-semibold">Live renewable assets and impact.</h1><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{[["Capacity",`${summary.capacityMw} MW`],["Live output",`${summary.currentOutputMw} MW`],["Annual generation",`${summary.annualGenerationMwh.toLocaleString("en-GB")} MWh`],["Carbon avoided",`${summary.avoidedCarbonTonnes.toLocaleString("en-GB")} tCO₂e`],["Operational",String(summary.operationalAssets)]].map(([l,v])=><article className="rounded-2xl border bg-white p-5" key={l}><small className="text-slate-500">{l}</small><strong className="mt-2 block text-2xl">{v}</strong></article>)}</div><div className="mt-7 grid gap-5 lg:grid-cols-3">{RENEWABLE_ASSETS.map(a=><article className="rounded-3xl border bg-white p-6" key={a.id}><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{a.type}</span><h2 className="mt-5 text-xl font-bold">{a.name}</h2><p className="mt-2 text-sm text-slate-500">{a.currentOutputMw} MW live output of {a.capacityMw} MW capacity.</p></article>)}</div></div></main><Footer/></>}
