
import { Header } from "@/components/layout/header";
import { Footer } from "@/components/layout/footer";
import { MARKETPLACE_ASSETS } from "@/data/marketplace";

export default function InvestmentsPage() {
  return <><Header/><main className="pc-shell min-h-[820px] px-5 py-14"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.16em] text-emerald-700">Investment portfolio</p><h1 className="mt-3 text-5xl font-semibold tracking-tight">Tokenized renewable positions.</h1><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Portfolio value","1 245 680,40 €"],["Annual income","84 240,00 €"],["Impact","4 820 tCO₂e"],["Active positions","12"]].map(([l,v])=><article className="rounded-2xl border bg-white p-5" key={l}><small className="text-slate-500">{l}</small><strong className="mt-2 block text-2xl">{v}</strong></article>)}</div><div className="mt-6 overflow-hidden rounded-3xl border bg-white"><table className="w-full min-w-[760px] text-left"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Asset","Category","Units","Value","Yield","Status"].map(h=><th className="px-5 py-3" key={h}>{h}</th>)}</tr></thead><tbody>{MARKETPLACE_ASSETS.map((asset,i)=><tr className="border-t" key={asset.id}><td className="px-5 py-4 font-semibold">{asset.title}</td><td className="px-5 py-4 capitalize">{asset.category}</td><td className="px-5 py-4">{(120+i*45).toLocaleString("fi-FI")}</td><td className="px-5 py-4">{((120+i*45)*asset.unitPriceEur).toLocaleString("fi-FI",{style:"currency",currency:"EUR"})}</td><td className="px-5 py-4">{asset.annualYieldPercent?`${asset.annualYieldPercent.toLocaleString("fi-FI")}%`:"—"}</td><td className="px-5 py-4 text-emerald-700">Active</td></tr>)}</tbody></table></div></div></main><Footer/></>
}
