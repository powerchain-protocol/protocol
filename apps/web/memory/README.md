
# AI memory

Client memory is opt-in and stored locally in the demo application. Production deployments should use encrypted, tenant-scoped storage with explicit retention, deletion, and consent controls.
