"use client";
import { FormEvent, useState } from "react";
import { useTerminal } from "@/hooks/use-terminal";
export function Terminal() { const {lines,execute}=useTerminal(); const [value,setValue]=useState(""); const submit=(event:FormEvent)=>{event.preventDefault();const command=value;setValue("");void execute(command);}; return <section className="rounded-2xl border border-emerald-500/20 bg-black p-4 font-mono text-sm text-emerald-300 shadow-2xl" aria-label="Powerchain developer terminal"><div className="mb-3 flex items-center justify-between border-b border-white/10 pb-3"><strong>Powerchain Terminal</strong><span className="text-xs text-white/50">interactive</span></div><div className="h-72 space-y-1 overflow-y-auto" role="log" aria-live="polite">{lines.map((line)=><div key={line.id} className={line.kind==="error"?"text-red-400":line.kind==="input"?"text-white":"text-emerald-300"}>{line.text}</div>)}</div><form onSubmit={submit} className="mt-3 flex gap-2 border-t border-white/10 pt-3"><label className="sr-only" htmlFor="terminal-command">Command</label><span>$</span><input id="terminal-command" value={value} onChange={(e)=>setValue(e.target.value)} autoComplete="off" className="min-w-0 flex-1 bg-transparent text-white outline-none" placeholder="help" /></form></section>; }
