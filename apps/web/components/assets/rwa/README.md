# RWA Components

Reusable UI components for tokenized real-world assets used by merchant renewables, P2P energy, carbon markets, connected hardware and the RWA exchange.

The UI never stores private keys. Trade buttons create client-side quote intent only; production order submission must pass wallet signing, asset verification, compliance policy and server-side settlement checks.
