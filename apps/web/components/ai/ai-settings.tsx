
"use client";import {useAi} from "@/context/ai-context";
export function AiSettings(){const {settings,setSettings}=useAi();return <section className="rounded-2xl border bg-white p-5"><h3 className="font-bold">AI settings</h3><label className="mt-4 block text-xs font-bold">Model<select value={settings.model} onChange={e=>setSettings({...settings,model:e.target.value})} className="mt-2 h-10 w-full rounded-xl border px-3"><option value="operations">Operations</option><option value="analytics">Analytics</option><option value="developer">Developer</option></select></label><label className="mt-4 flex items-center justify-between text-sm"><span>Memory</span><input type="checkbox" checked={settings.memoryEnabled} onChange={e=>setSettings({...settings,memoryEnabled:e.target.checked})}/></label><label className="mt-3 flex items-center justify-between text-sm"><span>Confirm actions</span><input type="checkbox" checked={settings.confirmActions} onChange={e=>setSettings({...settings,confirmActions:e.target.checked})}/></label></section>}
