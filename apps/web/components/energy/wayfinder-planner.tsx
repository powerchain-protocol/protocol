"use client";
import { useMemo, useState } from "react";
import { ENERGY_MARKETS } from "@/constants/energy-market";
import { buildWayfinderRoute } from "@/lib/energy/marketplace";
export function WayfinderPlanner(){const [kwh,setKwh]=useState(500);const route=useMemo(()=>buildWayfinderRoute(ENERGY_MARKETS,kwh),[kwh]);return <div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-2xl border border-white/10 bg-white/5 p-5"><label className="text-sm text-slate-300">Energy requirement (kWh)<input type="number" min="1" value={kwh} onChange={e=>setKwh(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-4 py-3"/></label><div className="mt-5 grid grid-cols-2 gap-3"><Metric label="Matched" value={`${route.estimatedKwh.toLocaleString()} kWh`}/><Metric label="Estimated cost" value={`€${route.estimatedCost.toFixed(2)}`}/><Metric label="Radius" value={`${route.totalDistanceKm.toFixed(1)} km`}/><Metric label="CO₂ avoided" value={`${route.avoidedKgCo2.toFixed(1)} kg`}/></div></div><div className="rounded-2xl border border-white/10 bg-white/5 p-5"><p className="text-sm text-emerald-300">Optimized route</p><div className="mt-4 space-y-3">{route.marketIds.map((id,index)=>{const m=ENERGY_MARKETS.find(x=>x.id===id)!;return <div key={id} className="flex items-center gap-4 rounded-xl border border-white/10 bg-slate-950 p-4"><span className="grid size-8 place-items-center rounded-full bg-emerald-400 font-bold text-slate-950">{index+1}</span><div className="flex-1"><strong>{m.name}</strong><p className="text-sm text-slate-400">{m.source} · €{m.pricePerKwh.toFixed(2)}/kWh · {m.distanceKm} km</p></div></div>})}</div></div></div>}
function Metric({label,value}:{label:string;value:string}){return <div className="rounded-xl bg-slate-950 p-3"><p className="text-xs text-slate-500">{label}</p><strong>{value}</strong></div>}
