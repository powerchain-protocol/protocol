"use client";
import { useEnergyMarkets } from "@/hooks/use-energy-markets";
import { quoteEnergy } from "@/lib/energy/marketplace";
import { LocalEnergyMap } from "./local-energy-map";
export function EnergyMarketplace(){const {markets,query,setQuery,maxDistanceKm,setMaxDistanceKm}=useEnergyMarkets();return <div className="space-y-6"><div className="grid gap-3 rounded-2xl border border-white/10 bg-white/5 p-4 md:grid-cols-[1fr_220px]"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search region, source or cooperative" className="rounded-xl border border-white/10 bg-slate-950 px-4 py-3"/><label className="text-sm text-slate-300">Radius: {maxDistanceKm} km<input className="mt-2 w-full" type="range" min="5" max="100" value={maxDistanceKm} onChange={e=>setMaxDistanceKm(Number(e.target.value))}/></label></div><LocalEnergyMap markets={markets}/><div className="grid gap-4 lg:grid-cols-2">{markets.map(m=>{const q=quoteEnergy(m,100);return <article key={m.id} className="rounded-2xl border border-white/10 bg-white/5 p-5"><div className="flex justify-between gap-4"><div><h3 className="font-semibold">{m.name}</h3><p className="text-sm text-slate-400">{m.verifiedMeters} verified smart meters · {m.carbonIntensity} gCO₂/kWh</p></div><span className="h-fit rounded-full bg-emerald-400/10 px-3 py-1 text-xs text-emerald-300">{m.status}</span></div><div className="mt-5 grid grid-cols-3 gap-3 text-sm"><div><p className="text-slate-500">100 kWh</p><strong>€{q.subtotal.toFixed(2)}</strong></div><div><p className="text-slate-500">Fee</p><strong>€{q.protocolFee.toFixed(2)}</strong></div><div><p className="text-slate-500">CO₂ avoided</p><strong>{q.avoidedKgCo2.toFixed(1)} kg</strong></div></div><button className="mt-5 w-full rounded-xl bg-emerald-400 px-4 py-3 font-semibold text-slate-950">Create trade intent</button></article>})}</div></div>}
