"use client";
import { useState } from "react";
import { ENERGY_PROJECTS } from "@/constants/energy-market";
export function FundingGrid({mode}:{mode:"donation"|"crowdfunding"}){const [message,setMessage]=useState("");return <div className="grid gap-5 lg:grid-cols-3">{ENERGY_PROJECTS.map(p=>{const progress=Math.min(100,p.raised/p.target*100);return <article key={p.id} className="rounded-2xl border border-white/10 bg-white/5 p-5"><p className="text-xs uppercase tracking-wider text-emerald-300">{p.source} · verified</p><h2 className="mt-2 text-lg font-semibold">{p.title}</h2><p className="mt-2 text-sm text-slate-400">{p.location}</p><div className="mt-5 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full bg-emerald-400" style={{width:`${progress}%`}}/></div><div className="mt-2 flex justify-between text-sm"><strong>€{p.raised.toLocaleString()}</strong><span className="text-slate-400">of €{p.target.toLocaleString()}</span></div><p className="mt-4 text-sm text-slate-300">{p.impact}</p><form className="mt-5" onSubmit={e=>{e.preventDefault();setMessage(`${mode==="donation"?"Donation":"Contribution"} intent created for ${p.title}`)}}><input name="amount" type="number" min="1" placeholder="Amount EUR" className="w-full rounded-xl border border-white/10 bg-slate-950 px-4 py-3"/><button className="mt-3 w-full rounded-xl bg-emerald-400 px-4 py-3 font-semibold text-slate-950">{mode==="donation"?"Donate":"Back project"}</button></form></article>})}{message&&<p className="lg:col-span-3 rounded-xl border border-emerald-400/20 bg-emerald-400/10 p-4 text-sm text-emerald-200">{message}. Wallet settlement is required before funds move.</p>}</div>}
