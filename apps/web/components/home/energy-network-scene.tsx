
"use client";import {useEffect,useRef} from "react";import * as THREE from "three";
export function EnergyNetworkScene(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const m=ref.current;if(!m)return;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(48,m.clientWidth/Math.max(m.clientHeight,1),.1,100);camera.position.z=8;const renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(m.clientWidth,m.clientHeight);m.appendChild(renderer.domElement);const group=new THREE.Group();scene.add(group);group.add(new THREE.Mesh(new THREE.IcosahedronGeometry(2.25,3),new THREE.MeshBasicMaterial({color:0x10b981,wireframe:true,transparent:true,opacity:.28})));group.add(new THREE.Mesh(new THREE.SphereGeometry(1.7,48,48),new THREE.MeshPhongMaterial({color:0x064e3b,emissive:0x10b981,emissiveIntensity:.2,transparent:true,opacity:.75})));const light=new THREE.PointLight(0xa3e635,22,20);light.position.set(3,3,5);scene.add(new THREE.AmbientLight(0xffffff,1.1),light);let f=0;const run=()=>{f=requestAnimationFrame(run);group.rotation.y+=.0025;group.rotation.x=Math.sin(Date.now()*.00025)*.1;renderer.render(scene,camera)};run();const resize=()=>{camera.aspect=m.clientWidth/Math.max(m.clientHeight,1);camera.updateProjectionMatrix();renderer.setSize(m.clientWidth,m.clientHeight)};addEventListener("resize",resize);return()=>{cancelAnimationFrame(f);removeEventListener("resize",resize);renderer.dispose();m.removeChild(renderer.domElement)}},[]);return <div ref={ref} className="h-full min-h-[430px] w-full"/>}
