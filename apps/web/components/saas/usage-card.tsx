export function UsageCard({label,value,limit}:{label:string;value:number;limit:number}){const pct=Math.min(100,Math.round(value/limit*100));return <article className="rounded-xl border bg-card p-5"><div className="flex justify-between"><span className="font-medium">{label}</span><span className="text-sm text-muted-foreground">{value.toLocaleString()} / {limit.toLocaleString()}</span></div><div className="mt-4 h-2 overflow-hidden rounded bg-muted"><div className="h-full bg-emerald-600" style={{width:`${pct}%`}}/></div></article>}
