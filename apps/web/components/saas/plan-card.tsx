import Link from "next/link"; import type { SaasPlan } from "@powerchain/saas";
export function PlanCard({plan,featured=false}:{plan:SaasPlan;featured?:boolean}){return <article className={`rounded-2xl border p-6 ${featured?"border-emerald-500 bg-emerald-500/5":"bg-card"}`}><h3 className="text-xl font-semibold">{plan.name}</h3><p className="mt-3 text-4xl font-bold">${plan.monthlyPrice}<span className="text-sm font-normal text-muted-foreground">/mo</span></p><ul className="my-6 space-y-2 text-sm text-muted-foreground"><li>{plan.projects} projects</li><li>{plan.members} members</li><li>{plan.apiRequests.toLocaleString()} API requests</li></ul><Link href={`/checkout?plan=${plan.id}`} className="inline-flex rounded-lg bg-emerald-600 px-4 py-2 text-sm font-semibold text-white">Choose {plan.name}</Link></article>}
