# Next.js Faucet Example

A minimal server-side example that imports the canonical logic from `@powerchain/foucets-program`. Use it in a route handler or server action. Never expose the faucet secret key to browser code.
