# @powerchain/protocol

Shared types, network definitions, exact fee calculations, operation quotes, and environment-based Solana program address resolution for Powerchain swap, bridge, checkout, payments, escrow, and crowdfunding clients.

Production addresses are intentionally not hard-coded. Configure and verify each program ID after deployment.
